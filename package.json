{
  "name": "gulp-cache-bust-meta",
  "description": "A Gulp plugin for hash based cache busting for static assets including a meta hash option",
  "version": "0.1.0",
  "repository": "jkphl/gulp-cache-bust-meta",
  "author": "Joschi Kuphal <joschi@kuphal.net> (https://jkphl.is)",
  "files": [
    "index.js"
  ],
  "keywords": [
    "gulpplugin",
    "css",
    "js",
    "cache",
    "bust"
  ],
  "dependencies": {

  },
  "devDependencies": {
    "gulp": "^3.9.1",
    "gulp-jshint": "^2.0.4",
    "jshint": "^2.9.4",
    "istanbul": "^0.4.5",
    "mocha": "^3.2.0",
    "mocha-lcov-reporter": "^1.2.0",
    "should": "^11.1.1",
    "stream-assert": "^2.0.3"
  },
  "scripts": {
    "test1": "gulp && istanbul test _mocha --report html -- test/*.js --reporter spec",
    "test": "istanbul test _mocha --report html -- test/*.js --reporter spec",
    "coveralls": "istanbul cover ./node_modules/mocha/bin/_mocha --report lcovonly -- -R spec && cat ./coverage/lcov.info | ./node_modules/coveralls/bin/coveralls.js && rm -rf ./coverage"
  },
  "engines": {
    "node": ">= 4.0"
  },
  "license": "MIT"
}
